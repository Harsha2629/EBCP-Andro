<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ebcp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EBCP"
        android:usesCleartextTraffic="true">
        <activity android:name=".WasteManagement"></activity>
        <activity android:name=".PitRegister" />
        <activity android:name=".PitManagement"></activity>
        <activity android:name=".CompostPit" />
        <activity android:name=".PitValues" />
        <activity android:name=".PitList" />
        <activity android:name=".AboutUs" />
        <activity android:name=".Recommendation" />
        <activity android:name=".SignUpActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
        <activity android:name=".Profile" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>